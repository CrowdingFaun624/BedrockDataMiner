/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        misc = @structures/misc/misc/
        temp = @structures/misc/template/
    }
}
base :StructureBase {
    log = @logs/structure_log
    this_types = #list
    structure = @files
    delegate = :Delegate {
        delegate_class = %DefaultBaseDelegate
        arguments = {"name" = "coverage"}
    }
}
files inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = @misc/string
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"sort" = %sort_by_value, "show_item_key" = false, "measure_length" = true, "field" = "unaccounted file"}
    }
}