/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        misc = @structures/misc/misc/
        temp = @structures/misc/template/
        s = @serializers/
    }
}
base inherit temp/base_pack_files {
    $ name = "language"
    $ pack_type = "resource pack"
    $ structure = @root
}
root inherit temp/SortedKeymap {
    keys = {
        "removed" = :Key {types = #list, structure = @removed}
        "renamed" = :Key {types = #dict, structure = @renamed}
    }
}
removed inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = @misc/string
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "key", "show_item_key" = false, "sort" = %sort_by_value, "measure_length" = true}
    }
}
renamed inherit temp/Dict {
    value_types = #str
    key_structure = @misc/string
    value_structure = @misc/string
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "key", "sort" = %sort_by_key, "measure_length" = true}
    }
}