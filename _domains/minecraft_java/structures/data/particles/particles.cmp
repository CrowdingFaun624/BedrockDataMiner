/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        a = @structures/misc/types/
        bs = @structures/misc/block_state/
        misc = @structures/misc/misc/
        temp = @structures/misc/template/
        vector = @structures/misc/vector/
    }
}
particle :Switch {
    switch_function = :Function {
        function = %get_key
        arguments = {"key" = "type"}
    }
    substructures = {
        "minecraft:angry_villager" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:ash" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:block" = :Key {types = @a/compound, structure = @particle.block_state}
        "minecraft:block_crumble" = :Key {types = @a/compound, structure = @particle.block_state}
        "minecraft:block_marker" = :Key {types = @a/compound, structure = @particle.block_state}
        "minecraft:bubble" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:bubble_column_up" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:bubble_pop" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:campfire_cozy_smoke" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:campfire_signal_smoke" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:cherry_leaves" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:cloud" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:composter" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:crimson_spore" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:crit" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:current_down" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:damage_indicator" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dolphin" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dragon_breath" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dripping_dripstone_lava" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dripping_dripstone_water" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dripping_honey" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dripping_lava" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dripping_obsidian_tear" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dripping_water" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:dust" = :Key {types = @a/compound, structure = @particle.dust}
        "minecraft:dust_color_transition" = :Key {types = @a/compound, structure = @particle.dust_color_transition}
        "minecraft:dust_pillar" = :Key {types = @a/compound, structure = @particle.block_state}
        "minecraft:dust_plume" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:effect" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:egg_crack" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:elder_guardian" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:electric_spark" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:enchant" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:enchanted_hit" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:end_rod" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:entity_effect" = :Key {types = @a/compound, structure = @particle.entity_effect}
        "minecraft:explosion" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:explosion_emitter" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_dripstone_lava" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_dripstone_water" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_dust" = :Key {types = @a/compound, structure = @particle.block_state}
        "minecraft:falling_honey" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_lava" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_nectar" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_obsidian_tear" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_spore_blossom" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:falling_water" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:firefly" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:firework" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:fishing" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:flame" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:flash" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:glow" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:glow_squid_ink" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:gust" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:gust_emitter_large" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:gust_emitter_small" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:happy_villager" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:heart" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:infested" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:instant_effect" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:item" = :Key {types = @a/compound, structure = @particle.item}
        "minecraft:item_cobweb" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:item_slime" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:item_snowball" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:landing_honey" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:landing_lava" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:landing_obsidian_tear" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:large_smoke" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:lava" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:mycelium" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:nautilus" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:note" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:ominous_spawning" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:pale_oak_leaves" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:poof" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:portal" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:raid_omen" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:rain" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:reverse_portal" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:scrape" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:sculk_charge" = :Key {types = @a/compound, structure = @particle.sculk_charge}
        "minecraft:sculk_charge_pop" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:sculk_soul" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:shriek" = :Key {types = @a/compound, structure = @particle.shriek}
        "minecraft:small_flame" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:small_gust" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:smoke" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:sneeze" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:snowflake" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:sonic_boom" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:soul" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:soul_fire_flame" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:spit" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:splash" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:spore_blossom_air" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:squid_ink" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:sweep_attack" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:tinted_leaves" = :Key {types = @a/compound, structure = @particle.tinted_leaves}
        "minecraft:totem_of_undying" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:trail" = :Key {types = @a/compound, structure = @particle.trail}
        "minecraft:trial_omen" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:trial_spawner_detection" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:trial_spawner_detection_ominous" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:underwater" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:vault_connection" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:vibration" = :Key {types = @a/compound, structure = @particle.vibration}
        "minecraft:warped_spore" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:wax_off" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:wax_on" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:white_ash" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:white_smoke" = :Key {types = @a/compound, structure = @particle.empty}
        "minecraft:witch" = :Key {types = @a/compound, structure = @particle.empty}
    }
}
particle:all {
    "type" = :Key {types = @a/string, delegate_arguments = {"always_print" = true}}
}
particle.block_state inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "block_state" = :Key {types = [@a/string, @a/compound], structure = @bs/block_state_or_id}
    }
}
particle.dust inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "color" = :Key {types = @a/list, structure = @misc/color_list}
        "scale" = :Key {types = @a/float}
    }
}
particle.dust_color_transition inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "from_color" = :Key {types = @a/list, structure = @misc/color_list}
        "to_color" = :Key {types = @a/list, structure = @misc/color_list}
        "scale" = :Key {types = @a/float}
    }
}
particle.empty inherit temp/SortedKeymap.nbt {
    keys = @particle:all
}
particle.entity_effect inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "color" = :Key {types = [@a/int, @a/list], structure = @misc/color}
    }
}
particle.item inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "item" = :Key {types = [@a/string, @a/compound], structure = @misc/item_type_or_id}
    }
}
particle.sculk_charge inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "roll" = :Key {types = @a/float}
    }
}
particle.shriek inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "delay" = :Key {types = @a/int}
    }
}
particle.tinted_leaves inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "color" = :Key {types = @a/int}
    }
}
particle.trail inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "target" = :Key {types = @a/list, structure = @vector/vector.tag_int} // type of value is not specified
        "color" = :Key {types = @a/int}
        "duration" = :Key {types = @a/int}
    }
}
particle.vibration inherit temp/SortedKeymap.nbt {
    keys = @particle:all + {
        "destination" = :Key {types = @a/compound, structure = @misc/position_source}
        "arrival_in_ticks" = :Key {types = @a/int}
    }
}