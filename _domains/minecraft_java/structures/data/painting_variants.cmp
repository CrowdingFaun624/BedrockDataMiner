/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        temp = @structures/misc/template/
        tc = @structures/misc/text_component/
    }
}
base inherit temp/base_multiple_pack_files {
    $ name = "painting_variants"
    $ pack_type = "data pack"
    $ types = #dict
    $ structure = @root
}
root inherit temp/SortedKeymap {
    keys = {
        "asset_id" = :Key {types = #str, delegate_arguments = {"always_print" = true}}
        "width" = :Key {types = #int}
        "height" = :Key {types = #int}
        "title" = :Key {types = @tc/root_type, structure = @tc/root}
        "author" = :Key {types = @tc/root_type, structure = @tc/root}
    }
}