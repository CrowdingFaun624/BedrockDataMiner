/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        misc = @structures/misc/misc/
        temp = @structures/misc/template/
        prov = @structures/misc/provider/int_provider/
    }
}
base inherit temp/base_multiple_pack_files {
    $ pack_type = "data pack"
    $ name = "dimension_types"
    $ structure = @root
}
root inherit temp/SortedKeymap {
    keys = {
        "ultrawarm" = :Key {types = #bool}
        "natural" = :Key {types = #bool}
        "coordinate_scale" = :Key {types = @misc/decimal}
        "has_skylight" = :Key {types = #bool}
        "has_ceiling" = :Key {types = #bool}
        "ambient_light" = :Key {types = @misc/decimal}
        "fixed_time" = :Key {types = @misc/decimal}
        "monster_spawn_light_level" = :Key {types = [#int, #dict], structure = @prov/int_provider}
        "monster_spawn_block_light_limit" = :Key {types = #int}
        "piglin_safe" = :Key {types = #bool}
        "bed_works" = :Key {types = #bool}
        "respawn_anchor_works" = :Key {types = #bool}
        "has_raids" = :Key {types = #bool}
        "logical_height" = :Key {types = #int}
        "cloud_height" = :Key {types = #int}
        "min_y" = :Key {types = #int}
        "height" = :Key {types = #int}
        "infiniburn" = :Key {types = #str}
        "effects" = :Key {types = #str}
    }
}