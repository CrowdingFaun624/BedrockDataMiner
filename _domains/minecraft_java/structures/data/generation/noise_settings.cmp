/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        a = @structures/misc/types/
        bs = @structures/misc/block_state/
        df = @structures/data/generation/density_function/
        misc = @structures/misc/misc/
        sr = @structures/data/generation/surface_rule/
        t = @structure_tags/
        temp = @structures/misc/template/
        vector = @structures/misc/vector/
    }
}
base inherit temp/base_multiple_pack_files {
    $ name = "noise_settings"
    $ pack_type = "data pack"
    $ structure = @noise_settings
}
noise_settings_or_id :Union {
    substructures = [
        :Key {types = #str},
        :Key {types = #dict, structure = @noise_settings},
    ]
}
noise_settings inherit temp/SortedKeymap {
    keys = {
        "sea_level" = :Key {types = #int}
        "disable_mob_generation" = :Key {types = #bool}
        "aquifers_enabled" = :Key {types = #bool}
        "noise_caves_enabled" = :Key {types = #bool, tags = @t/outdated}
        "noodle_caves_enabled" = :Key {types = #bool, tags = @t/outdated}
        "ore_veins_enabled" = :Key {types = #bool}
        "legacy_random_source" = :Key {types = #bool}
        "default_block" = :Key {types = @a/compound, structure = @bs/block_state}
        "default_fluid" = :Key {types = @a/compound, structure = @bs/block_state}
        "spawn_target" = :Key {types = #list, structure = @spawn_target}
        "noise" = :Key {types = #dict, structure = @noise}
        "noise_router" = :Key {types = #dict, structure = @noise_router}
        "surface_rule" = :Key {types = #dict, structure = @sr/surface_rule}
        "structures" = :Key {types = #dict, structure = @structures, tags = @t/outdated}
    }
}
spawn_target inherit temp/Set {
    $ field = "range"
    value_types = #dict
    value_structure = @noise_parameter
}
noise_parameter inherit temp/SortedKeymap {
    keys = {
        "temperature" = :Key {types = [#dict, #list, #float], structure = @spawn_target:range}
        "humidity" = :Key {types = [#dict, #list, #float], structure = @spawn_target:range}
        "continentalness" = :Key {types = [#dict, #list, #float], structure = @spawn_target:range}
        "erosion" = :Key {types = [#dict, #list, #float], structure = @spawn_target:range}
        "weirdness" = :Key {types = [#dict, #list, #float], structure = @spawn_target:range}
        "depth" = :Key {types = [#dict, #list, #float], structure = @spawn_target:range}
        "offset" = :Key {types = #float}
    }
}
spawn_target:range :Union {
    substructures = [
        :Key {types = #float},
        :Key {types = #dict, structure = inherit temp/SortedKeymap {
            keys = {
                "min" = :Key {types = #float}
                "max" = :Key {types = #float}
            }
        }},
        :Key {types = #list, structure = inherit temp/List {
            $ print_all = true
            $ print_flat = true
            $ field = "range item"
            value_types = #float
            value_structure = null
        }},
    ]
}
noise inherit temp/SortedKeymap {
    keys = {
        "min_y" = :Key {types = #int}
        "height" = :Key {types = #int}
        "size_horizontal" = :Key {types = #int}
        "size_vertical" = :Key {types = #int}
        "amplified" = :Key {types = #bool, tags = @t/outdated}
        "large_biomes" = :Key {types = #bool, tags = @t/outdated}
        "island_noise_override" = :Key {types = #bool, tags = @t/outdated}
        "top_slide" = :Key {types = #dict, structure = @slide, tags = @t/outdated}
        "bottom_slide" = :Key {types = #dict, structure = @slide, tags = @t/outdated}
        "sampling" = :Key {types = #dict, structure = @sampling, tags = @t/outdated}
        "terrain_shaper" = :Key {types = #dict, structure = @terrain_shaper, tags = @t/outdated}
    }
}
noise_router inherit temp/SortedKeymap {
    keys = {
        "initial_density_without_jaggedness" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "final_density" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "barrier" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "fluid_level_floodedness" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "fluid_level_spread" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "lava" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "vein_toggle" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "vein_ridged" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "vein_gap" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "temperature" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "vegetation" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "continents" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "erosion" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "depth" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "ridges" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
        "preliminary_surface_level" = :Key {types = [#str, @df/density_function_type], structure = @df/density_function_or_id}
    }
}
sampling inherit temp/SortedKeymap {
    keys = @df/density_function.old_blended_noise:keys
}
slide inherit temp/SortedKeymap {
    keys = {
        "target" = :Key {types = #float}
        "size" = :Key {types = #int}
        "offset" = :Key {types = #int}
    }
}
structures :Switch {
    switch_function = :Function {
        function = %noise_settings_structure_switch
    }
    substructures = {
        "old" = :Key {types = #dict, structure = @structures:structures, tags = @t/outdated}
        "new" = :Key {types = #dict, structure = @structures:new}
    }
}
structures:new inherit temp/SortedKeymap {
    keys = {
        "structures" = :Key {types = #dict, structure = @structures:structures}
        "stronghold" = :Key {types = #dict, structure = @structures:stronghold}
    }
}
structures:stronghold inherit temp/SortedKeymap {
    keys = {
        "distance" = :Key {types = #int}
        "spread" = :Key {types = #int}
        "count" = :Key {types = #int}
    }
}
structures:structures inherit temp/Dict {
    $ field = "structure"
    value_types = #dict
    value_structure = @placement
}
placement :Switch {
    switch_function = :Function {
        function = %noise_settings_placement_switch
    }
    substructures = {
        "new" = :Key {types = #dict, structure = @placement:new}
        "minecraft:random_spread" = :Key {types = #dict, structure = @placement.random_spread, tags = @t/outdated}
        "minecraft:concentric_rings" = :Key {types = #dict, structure = @placement.concentric_rings, tags = @t/outdated}
    }
}
placement:new inherit temp/SortedKeymap {
    keys = {
        "spacing" = :Key {types = #int}
        "separation" = :Key {types = #int}
        "salt" = :Key {types = #int}
    }
}
placement:all {
    "type" = :Key {types = #str, delegate_arguments = {"always_print" = true}}
}
placement.concentric_rings inherit temp/SortedKeymap {
    keys = @placement:all + {
        "distance" = :Key {types = #int}
        "spread" = :Key {types = #int}
        "count" = :Key {types = #int}
    }
}
placement.random_spread inherit temp/SortedKeymap {
    keys = @placement:all + {
        "spread_type" = :Key {types = #str}
        "salt" = :Key {types = #int}
        "spacing" = :Key {types = #int}
        "separation" = :Key {types = #int}
        "locate_offset" = :Key {types = #list, structure = @vector/vector.int}
    }
}
terrain_shaper inherit temp/SortedKeymap {
    keys = {
        "offset" = :Key {types = [#dict, #float], structure = @terrain_shaper:offset}
        "factor" = :Key {types = [#dict, #float], structure = @terrain_shaper:offset}
        "jaggedness" = :Key {types = [#dict, #float], structure = @terrain_shaper:offset}
    }
}
terrain_shaper:offset :Union {
    substructures = [
        :Key {types = #float}
        :Key {types = #dict, structure = @df/density_function.spline:spline.keymap}
    ]
}