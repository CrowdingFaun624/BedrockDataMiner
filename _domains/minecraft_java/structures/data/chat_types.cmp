/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        misc = @structures/misc/misc/
        t = @structure_tags/
        temp = @structures/misc/template/
        tc = @structures/misc/text_component/
    }
}
base inherit temp/base_multiple_pack_files {
    $ name = "chat_types"
    $ pack_type = "data pack"
    $ types = #dict
    $ structure = @root
}
root inherit temp/SortedKeymap {
    keys = {
        "chat" = :Key {types = #dict, structure = @chat}
        "narration" = :Key {types = #dict, structure = @chat}
        "overlay" = :Key {types = #dict, structure = @misc/empty_dict, tags = @t/outdated}
    }
}
chat :Switch {
    switch_function = :Function {
        function = %chat_types_switch
    },
    substructures = {
        "old" = :Key {types = #dict, structure = @chat.old, tags = @t/outdated}
        "new" = :Key {types = #dict, structure = @chat.new}
    }
}
chat.old inherit temp/SortedKeymap {
    keys = {
        "priority" = :Key {types = #str, delegate_arguments = {"always_print" = true}}
        "decoration" = :Key {types = #dict, structure = @chat.new}
    }
}
chat.new inherit temp/SortedKeymap {
    keys = {
        "translation_key" = :Key {types = #str, delegate_arguments = {"always_print" = true}}
        "parameters" = :Key {types = #list, structure = @parameters}
        "style" = :Key {types = #dict, structure = @tc/text_component:formatting:keymap}
    }
}
parameters inherit temp/List {
    $ field = "parameter"
    value_types = #str
    value_structure = @misc/string
}