/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        misc = @structures/misc/misc/
        temp = @structures/misc/template/
    }
}
base :StructureBase {
    log = @logs/structure_log
    this_types = #dict
    structure = @root
    delegate = :Delegate {
        delegate_class = %DefaultBaseDelegate
        arguments = {"name" = "packs"}
    }
}
root :Normalizer {
    outer_types = #dict
    inner_types = #dict
    functions = :Function {
        function = %scripts/normalizers/normalizers/packs_normalizer
    }
    structure = inherit temp/SortedKeymap {
        keys = {
            "packs" = :Key {types = #list, structure = @pack_list{$field="pack"}}
            "namespaces" = :Key {types = #list, structure = @pack_list{$field="namespace"}}
        }
    }
}
pack_list inherit temp/Set {
    value_types = #str
    value_structure = @misc/string
}