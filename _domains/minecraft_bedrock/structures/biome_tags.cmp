/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        temp = @structures/template/
    }
}
base :StructureBase {
    this_types = #list
    structure = @biome_tags
    delegate = :Delegate {
        delegate_class = %DefaultBaseDelegate
        arguments = {"name" = "biome_tags"}
    }
}
biome_tags inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = null
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "biome tag", "show_item_key" = false, "sort" = %sort_by_value, "measure_length" = true}
    }
}