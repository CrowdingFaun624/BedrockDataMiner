/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_multiple_pack_files {
    $ name = "shadows"
    $ file_field = "shadows file"
    $ pack_type = "resource pack"
    $ structure = @shadows_file
}
shadows_file inherit temp/SortedKeymap {
    keys = {
        "format_version" = :Key {types = #str}
        "minecraft:shadow_settings" = :Key {types = #dict, structure = @shadow_settings, similarity_weight = 15}
    }
}
shadow_settings inherit temp/SortedKeymap {
    keys = {
        "shadow_style" = :Key {types = #str}
        "texel_size" = :Key {types = #int}
    }
}