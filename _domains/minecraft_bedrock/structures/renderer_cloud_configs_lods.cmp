/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_multiple_files {
    $ name = "renderer_cloud_configs_lods"
    $ structure = @renderer_cloud_configs_lod
}
renderer_cloud_configs_lod inherit temp/SortedKeymap {
    keys = {
        "parent" = :Key {types = #dict, structure = @structures/renderer_cloud_configs/file_link}
        "overrides" = :Key {types = #dict, structure = @renderer_cloud_configs_lod}
        "cloud_lighting_parameters" = :Key {types = #dict, structure = @cloud_lighting_parameters}
        "grid_size" = :Key {types = #int}
        "cloud_mesh_size" = :Key {types = #int}
        "cloud_render_distance_scale" = :Key {types = #float}
        "enable_cloud_render_distance" = :Key {types = #bool}
    }
}
cloud_lighting_parameters inherit temp/SortedKeymap {
    keys = {
        "cloud_lighting_enabled" = :Key {types = #bool}
    }
}