/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        temp = @structures/template/
    }
}
base :StructureBase {
    log = @logs/structure_log
    this_types = #dict
    structure = @pieces
    delegate = :Delegate {
        delegate_class = %DefaultBaseDelegate
        arguments = {"name" = "pieces_texts"}
    }
}
pieces inherit temp/Dict {
    value_types = #dict
    value_structure = @languages
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"sort" = %sort_by_key, "field" = "piece", "measure_length" = true}
    }
}
languages inherit temp/Dict {
    value_types = #file
    value_structure = @structures/languages_contents/cache_keys
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"sort" = %sort_by_key, "field" = "language", "measure_length" = true}
    }
}