/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_pack_files {
    $ name = "crafting_item_catalog"
    $ pack_type = "behavior pack"
    $ structure = @file
}
file inherit temp/SortedKeymap {
    keys = {
        "format_version" = :Key {types = #str, similarity_weight = 0}
        "minecraft:crafting_items_catalog" = :Key {types = #dict, structure = @crafting_items_catalog}
    }
}
crafting_items_catalog inherit temp/SortedKeymap {
    keys = {
        "categories" = :Key {types = #list, structure = @categories}
    }
}
categories inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #dict
    value_structure = @category
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "category", "show_item_key" = false, "measure_length" = true}
    }
}
category inherit temp/SortedKeymap {
    keys = {
        "category_name" = :Key {types = #str, structure = @temp/string, similarity_weight = 5, delegate_arguments = {"always_print" = true}}
        "groups" = :Key {types = #list, structure = @groups}
    }
}
groups inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #dict
    value_structure = @group
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "group", "show_item_key" = false, "measure_length" = true}
    }
}
group inherit temp/SortedKeymap {
    keys = {
        "group_identifier" = :Key {types = #dict, structure = @group_identifier, delegate_arguments = {"always_print" = true}}
        "items" = :Key {types = #list, structure = @items_list}
    }
}
group_identifier inherit temp/SortedKeymap {
    keys = {
        "icon" = :Key {types = #str, structure = @temp/string, delegate_arguments = {"always_print" = true}}
        "name" = :Key {types = #str, structure = @temp/string, delegate_arguments = {"always_print" = true}, similarity_weight = 2}
    }
}
items_list inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = @temp/string
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "item", "show_item_key" = false, "measure_length" = true}
    }
}