/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_multiple_files {
    $ name = "render_distance_configs"
    $ structure = @render_distance_config
}
render_distance_config inherit temp/SortedKeymap {
    keys = {
        "deferred_render_distance_configuration" = :Key {types = #dict, structure = @file_link}
        "default_deferred_distance" = :Key {types = #int}
    }
}
file_link inherit temp/SortedKeymap {
    keys = {
        "file" = :Key {types = #str}
    }
}