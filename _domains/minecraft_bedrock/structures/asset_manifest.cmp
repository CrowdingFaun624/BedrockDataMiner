/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_single_file {
    $ name = "asset_manifest"
    $ structure = @asset_manifest
}
asset_manifest inherit temp/SortedKeymap {
    keys = {
        "file" = :Key {types = #str}
        "hash" = :Key {types = #str}
        "size" = :Key {types = #int}
    }
}