/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_multiple_pack_files {
    $ name = "camera_lists"
    $ file_field = "camera list"
    $ pack_type = "behavior pack"
    $ structure = @camera_list
}
camera_list inherit temp/SortedKeymap {
    keys = {
        "format_version" = :Key {types = #str}
        "minecraft:camera_list" = :Key {types = #dict, similarity_weight = 16, structure = inherit temp/SortedKeymap {
            keys = {
                "identifier" = :Key {types = #str, structure = @temp/string}
                "cameras" = :Key {types = #list, structure = @cameras}
            }
        }}
    }
}
cameras inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = @temp/string
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "camera", "show_item_key" = false, "measure_length" = true}
    }
}