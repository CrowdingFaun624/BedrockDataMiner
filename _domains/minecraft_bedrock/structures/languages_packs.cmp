/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @minecraft_common!serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_pack_files {
    $ name = "languages_packs"
    $ types = #list
    $ structure = @languages
}
cache :Cache { // for other Groups
    this_types = #file
    structure = :File {
        serializer = @s/json
        outer_types = #file
        inner_types = #list
        structure = @languages
    }
}
languages inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = @temp/string
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "item", "show_item_key" = false, "sort" = %sort_by_value}
    }
}