/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_multiple_files {
    $ name = "renderer_deferred_lighting_configs"
    $ structure = @renderer_deferred_lighting_config
}
renderer_deferred_lighting_config inherit temp/SortedKeymap {
    keys = {
        "mode" = :Key {types = #str}
        "upscale_factor" = :Key {types = #float}
        "local_exposure" = :Key {types = #bool}
        "mixed_res_enabled" = :Key {types = #bool}
        "biome_blending" = :Key {types = #bool}
    }
}