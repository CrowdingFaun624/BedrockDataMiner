/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        temp = @structures/template/
    }
}
base :StructureBase {
    log = @logs/structure_log
    this_types = #list
    structure = @entity_components
    delegate = :Delegate {
        delegate_class = %DefaultBaseDelegate
        arguments = {"name" = "entity_components"}
    }
}
entity_components inherit temp/Dict {
    key_weight = 0
    key_types = #int
    this_types = #list
    value_types = #str
    value_structure = null
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "component", "show_item_key" = false, "sort" = %sort_by_value, "measure_length" = true}
    }
}