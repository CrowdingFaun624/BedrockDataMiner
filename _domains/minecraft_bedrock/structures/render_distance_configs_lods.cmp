/* Converted by CmpMigrator 0.1.9 */
settings {
    aliases {
        s = @serializers/
        temp = @structures/template/
    }
}
base inherit temp/base_multiple_files {
    $ name = "render_distance_configs_lods"
    $ structure = @render_distance_config_lod
}
render_distance_config_lod inherit temp/SortedKeymap {
    keys = {
        "render_distance_levels" = :Key {types = #list, structure = @render_distances}
    }
}
render_distances inherit temp/Dict {
    key_weight = 1
    value_weight = 2
    this_types = #list
    key_types = #int
    value_types = #int
    value_structure = @temp/number {
        normal_value = 2
    }
    delegate = :Delegate {
        delegate_class = %DefaultDelegate
        arguments = {"field" = "render distance", "measure_length" = true}
    }
}