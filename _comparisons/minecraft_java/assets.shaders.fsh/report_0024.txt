Difference of "text_packs" between "23w05a" (snapshot of "1.19.4") and "23w06a" (snapshot of "1.19.4").

Changed resource pack "minecraft":
	Total file: 85 (+2, -0)
	Added file "core/rendertype_text_background.fsh":
		'''
		#version 150
		
		#moj_import <fog.glsl>
		
		uniform sampler2D Sampler0;
		
		uniform vec4 ColorModulator;
		uniform float FogStart;
		uniform float FogEnd;
		uniform vec4 FogColor;
		
		in float vertexDistance;
		in vec4 vertexColor;
		in vec2 texCoord0;
		
		out vec4 fragColor;
		
		void main() {
		    vec4 color = vertexColor * ColorModulator;
		    if (color.a < 0.1) {
		        discard;
		    }
		    fragColor = linear_fog(color, vertexDistance, FogStart, FogEnd, FogColor);
		}
		'''
	Added file "core/rendertype_text_background_see_through.fsh":
		'''
		#version 150
		
		uniform vec4 ColorModulator;
		
		in vec4 vertexColor;
		
		out vec4 fragColor;
		
		void main() {
		    vec4 color = vertexColor;
		    if (color.a < 0.1) {
		        discard;
		    }
		    fragColor = color * ColorModulator;
		}
		'''